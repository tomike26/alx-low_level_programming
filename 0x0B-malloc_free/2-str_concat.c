#include <stdlib.h>
#include "main.h"

/**
 * *str_concat - c function that catenates two strings
 *
 * @s1: string to concatenate
 * @s2: other string to concatenate
 *
 * Return: pointer to the new string created (Success), or NULL (Failed)
 */
char *str_concat(char *s1, char *s2)
